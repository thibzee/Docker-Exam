export declare class HotFile {
    #private;
    constructor(path: string);
    /**
     * Write the hot file
     */
    write(data: {
        url: string;
    }): Promise<void>;
    /**
     * Delete the hot file
     */
    clean(): void;
}
